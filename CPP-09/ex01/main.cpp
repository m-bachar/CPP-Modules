#include "RPN.hpp"

int	main(int ac, char *av[])
{
	if (ac != 2)
	{
		std::cout << RED << " * Error : " << RESET << "invalid arguments count" << std::endl;
		return 1;
	}
	RPN	rpn;

	std::string	input;
}